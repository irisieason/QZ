# GitHub Packages 发布配置
# 
# 使用说明：
# 1. 复制此文件为 .env
# 2. 将 YOUR_GITHUB_TOKEN 替换为你的实际 GitHub Token
# 3. .env 文件不会被提交到 Git（已在 .gitignore 中）

# GitHub Personal Access Token
# 创建 Token: https://github.com/settings/tokens
# 需要权限: write:packages, read:packages, repo
NODE_AUTH_TOKEN=YOUR_GITHUB_TOKEN

# 可选：NPM Registry（默认使用 GitHub Packages）
# NPM_REGISTRY=https://npm.pkg.github.com
