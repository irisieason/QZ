# GitHub Packages Registry Configuration
@irisieason:registry=https://npm.pkg.github.com

# 认证配置（支持多种方式）
# 
# 方法 1: 使用 .env 文件（推荐用于本地开发）
# 在项目根目录的 .env 文件中设置:
# NODE_AUTH_TOKEN=your_github_token
# 
# 方法 2: 使用环境变量（推荐用于 CI/CD）
# Mac/Linux: export NODE_AUTH_TOKEN=your_github_token
# Windows PowerShell: $env:NODE_AUTH_TOKEN="your_github_token"
# Windows CMD: set NODE_AUTH_TOKEN=your_github_token
# 
# 方法 3: 使用全局 ~/.npmrc 配置（推荐用于个人开发）
# 在 ~/.npmrc 中添加:
# //npm.pkg.github.com/:_authToken=YOUR_GITHUB_TOKEN
# @irisieason:registry=https://npm.pkg.github.com
# 
# 注意：
# - .env 文件已在 .gitignore 中，不会被提交到 Git
# - 环境变量优先级：命令行 > .env 文件 > 全局 ~/.npmrc
# - 详细配置说明请查看 PUBLISH_GUIDE.md

//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
